// Shared constants for the groups feature, importable by any layer (hooks, UI)
// without creating a dependency between them.

// Sentinel for the "no starter pack" choice in the create-group dialog.
export const BLANK_PACK = 'blank';
